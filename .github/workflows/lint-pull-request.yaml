name: PR / Lint

on:
  pull_request:
    branches:
     - main

permissions:
  contents: read

env:
  LEIN_USE_BOOTCLASSPATH: no

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/actions/setup-clojure-project

      - uses: ./.github/actions/lint-clojure-project
